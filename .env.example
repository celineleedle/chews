# Server configuration. Copy to .env — the web client has no env vars at all.
PORT=8787
NODE_ENV=development

# Google Places (server-side only, never shipped to the client)
GOOGLE_PLACES_API_KEY=
# 1 = serve the built-in mock restaurant deck (offline dev)
MOCK_PLACES=1
PLACES_CACHE_TTL_MIN=45
